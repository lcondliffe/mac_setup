- name: Configure macOS screenshot symbolic hotkeys
  ansible.builtin.shell:
    cmd: |
      set -uo pipefail
      id="{{ item.id }}"
      desired="{{ item.parameters | join(',') }}"
      current=$(defaults export com.apple.symbolichotkeys - 2>/dev/null \
        | plutil -extract "AppleSymbolicHotKeys.${id}.value.parameters" json -o - - 2>/dev/null \
        | tr -d '[:space:][]') || true
      if [ "$current" = "$desired" ]; then
        echo "UNCHANGED"
        exit 0
      fi
      defaults write com.apple.symbolichotkeys AppleSymbolicHotKeys -dict-add "$id" "
        <dict>
          <key>enabled</key><true/>
          <key>value</key>
          <dict>
            <key>type</key><string>standard</string>
            <key>parameters</key>
            <array>
              <integer>{{ item.parameters[0] }}</integer>
              <integer>{{ item.parameters[1] }}</integer>
              <integer>{{ item.parameters[2] }}</integer>
            </array>
          </dict>
        </dict>"
      echo "CHANGED"
  register: symbolichotkeys_result
  changed_when: (symbolichotkeys_result.stdout | trim) == 'CHANGED'
  loop: "{{ screenshot_hotkeys }}"
  loop_control:
    label: "{{ item.name }}"
  tags: [keyboard, shortcuts]

# macOS claims fn first, so an app bound to it silently never fires.
- name: Set what the fn/globe key does on its own
  community.general.osx_defaults:
    domain: com.apple.HIToolbox
    key: AppleFnUsageType
    type: int
    value: "{{ fn_key_usage }}"
    state: present
  register: fn_key_result
  tags: [keyboard, shortcuts]

- name: Reactivate symbolic hotkeys (apply without logout)
  ansible.builtin.command: >-
    /System/Library/PrivateFrameworks/SystemAdministration.framework/Resources/activateSettings -u
  when: symbolichotkeys_result.changed or fn_key_result.changed
  changed_when: false
  tags: [keyboard, shortcuts]
